.idea
__pycache__
data/__pycache__
models/__pycache__
utils/__pycache__
model_trained
output
vlm

# HuggingFace model snapshots auto-downloaded by config.set_hf_cache_env().
# Keep the folder in the repo (so it exists on fresh clones) but ignore
# every file inside it.
pretrained_lm/*
!pretrained_lm/.gitkeep
